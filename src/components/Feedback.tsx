import { useMemo, useState } from "react"; import { Star } from "lucide-react";
type F={rating:number;comment:string;createdAt:string}; const key="talentsphere_feedback"; const all=():F[]=>{try{return JSON.parse(localStorage.getItem(key)||"[]")}catch{return []}};
export default function Feedback(){const [items,setItems]=useState<F[]>(all);const [rating,setRating]=useState(0);const [comment,setComment]=useState("");const avg=useMemo(()=>items.length?(items.reduce((s,x)=>s+x.rating,0)/items.length).toFixed(1):"0.0",[items]);const submit=()=>{if(!rating||!comment.trim())return;const next=[{rating,comment:comment.trim(),createdAt:new Date().toISOString()},...items];localStorage.setItem(key,JSON.stringify(next));setItems(next);setRating(0);setComment("")};return <section id="feedback" className="bg-slate-50 py-20"><div className="mx-auto max-w-6xl px-6"><div className="text-center"><span className="rounded-full bg-cyan-100 px-4 py-2 text-sm font-semibold text-cyan-700">Feedback</span><h2 className="mt-5 text-4xl font-bold">Real user feedback</h2><p className="mt-3 text-slate-600">Overall rating: <b>{avg}/5</b> · {items.length} review(s)</p></div><div className="mt-10 grid gap-6 lg:grid-cols-2"><div className="rounded-2xl border bg-white p-6"><h3 className="text-xl font-bold">Share your experience</h3><div className="my-5 flex gap-2">{[1,2,3,4,5].map(n=><button key={n} onClick={()=>setRating(n)} aria-label={`${n} stars`}><Star className={n<=rating?'fill-yellow-400 text-yellow-400':'text-slate-300'}/></button>)}</div><textarea value={comment} onChange={e=>setComment(e.target.value)} placeholder="Write your feedback..." className="min-h-36 w-full rounded-xl border p-4"/><button onClick={submit} className="mt-4 rounded-xl bg-cyan-600 px-5 py-3 font-semibold text-white">Submit feedback</button></div><div className="space-y-3">{items.length?items.slice(0,5).map((x,i)=><div key={i} className="rounded-2xl border bg-white p-5"><p className="font-semibold">{'★'.repeat(x.rating)}{'☆'.repeat(5-x.rating)}</p><p className="mt-2 text-slate-600">{x.comment}</p></div>):<div className="rounded-2xl border bg-white p-6 text-slate-600">No fabricated testimonials. Submitted feedback will appear here.</div>}</div></div></div></section>}
