import type{CollegeProfileData}from'./collegeProfileService';export type ResumeData={fullName:string;email:string;phone:string;address:string;linkedin:string;github:string;portfolio:string;summary:string;skills:string;education:string;experience:string;projects:string;certifications:string;achievements:string;internships:string};export type ResumeAnalysis={resumeScore:number;atsScore:number;roleMatch:number;sections:Record<string,number>;strengths:string[];improvements:string[];missingRoleSkills:string[]};
const empty:ResumeData={fullName:'',email:'',phone:'',address:'',linkedin:'',github:'',portfolio:'',summary:'',skills:'',education:'',experience:'',projects:'',certifications:'',achievements:'',internships:''};const key=(u:string)=>`college_resume_${u||'guest'}`;export const getResume=(u:string):ResumeData=>{try{return{...empty,...JSON.parse(localStorage.getItem(key(u))||'{}')}}catch{return{...empty}}};
const roleSkills:Record<string,string[]>={'software developer':['programming','dsa','oop','dbms','git','api'],'data analyst':['python','sql','excel','statistics','power bi'],'ai engineer':['python','machine learning','statistics','deep learning','sql'],'cloud engineer':['cloud','linux','networking','docker','git'],'cybersecurity analyst':['networking','linux','security','python'],'core engineering role':['project','design','technical','quality']};
export function analyzeResume(r:ResumeData,p:CollegeProfileData):ResumeAnalysis{const sections={Contact:r.email&&r.phone?100:r.email||r.phone?50:0,Summary:r.summary.length>=80?100:r.summary?60:0,Education:r.education?100:0,Skills:r.skills?100:0,Projects:r.projects.length>=80?100:r.projects?60:0,Experience:(r.experience||r.internships)?100:0,Links:(r.linkedin?34:0)+(r.github?33:0)+(r.portfolio?33:0)};const vals=Object.values(sections),resumeScore=Math.round(vals.reduce((a,b)=>a+b,0)/vals.length),text=Object.values(r).join(' ').toLowerCase(),expected=roleSkills[p.targetRole.toLowerCase()]||[],found=expected.filter(s=>text.includes(s)),missingRoleSkills=expected.filter(s=>!text.includes(s)),roleMatch=expected.length?Math.round(found.length/expected.length*100):0,atsScore=Math.min(100,Math.round(resumeScore*.7+roleMatch*.3));const strengths=Object.entries(sections).filter(([,v])=>v===100).map(([k])=>`${k} section is complete`),improvements=Object.entries(sections).filter(([,v])=>v<100).map(([k])=>`Improve ${k.toLowerCase()} section`);if(!p.targetRole)improvements.unshift('Select a target role in your College profile for role-match analysis');else if(missingRoleSkills.length)improvements.unshift(`Add genuine evidence for target-role skills: ${missingRoleSkills.join(', ')}`);return{resumeScore,atsScore,roleMatch,sections,strengths,improvements,missingRoleSkills}}
export const saveResume=(u:string,v:ResumeData,p:CollegeProfileData)=>{localStorage.setItem(key(u),JSON.stringify(v));let h:any[]=[];try{h=JSON.parse(localStorage.getItem(`college_resume_history_${u||'guest'}`)||'[]')}catch{}h.push({id:crypto.randomUUID(),savedAt:new Date().toISOString(),analysis:analyzeResume(v,p)});localStorage.setItem(`college_resume_history_${u||'guest'}`,JSON.stringify(h))};
