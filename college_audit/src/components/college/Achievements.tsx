import{useAuth}from'../../contexts/AuthContext';import{getCollegeStats}from'../../services/collegeService';export default function Achievements(){const{user}=useAuth();const s=getCollegeStats(user?.id||'guest');const badges=[['Assessment Starter',s.attempts.length>0],['Assessment Improver',s.attempts.length>1&&s.attempts.at(-1)!.percentage>s.attempts[0].percentage],['Resume Ready',s.resume.resumeScore>=70],['ATS Focused',s.resume.atsScore>=80],['Practice Builder',s.practicePct===100],['Interview Starter',s.interviewScore>0],['Placement Tracker',s.placements.length>0],['Placement Ready',s.readiness>=75]];return <div className="space-y-5"><div><h1 className="text-3xl font-bold">Achievements</h1><p className="mt-1 text-slate-600">Badges unlock only from actual College activity.</p></div><div className="grid gap-4 md:grid-cols-2 xl:grid-cols-3">{badges.map(([t,ok])=><div key={String(t)} className={`rounded-2xl border bg-white p-5 ${ok?'':'opacity-50'}`}><div className="text-3xl">🏆</div><h2 className="mt-3 text-xl font-bold">{t}</h2><p className="mt-2 text-sm text-slate-500">{ok?'Unlocked from your activity.':'Locked — continue your College journey.'}</p></div>)}</div></div>}
