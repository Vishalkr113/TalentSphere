import{useAuth}from'../../contexts/AuthContext';import{getCollegeProfile,getCollegeStats,getSkillGap,isCollegeProfileReady}from'../../services/collegeService';import CollegeProfileRequired from'./CollegeProfileRequired';export default function LearningRoadmap(){const{user}=useAuth();const id=user?.id||'guest',p=getCollegeProfile(id),s=getCollegeStats(id),gaps=getSkillGap(id).slice(0,4);if(!isCollegeProfileReady(p))return <CollegeProfileRequired/>;return <div className="space-y-5"><div><h1 className="text-3xl font-bold">Personalized Learning Roadmap</h1><p className="mt-1 text-slate-600">Built from current {p.branch} assessment and resume evidence{p.targetRole?` for ${p.targetRole}`:''}.</p></div>{!s.attempts.length?<section className="rounded-2xl border bg-white p-6 text-slate-600">Complete an assessment first so the roadmap can prioritize actual weak skills instead of showing a generic plan.</section>:<div className="space-y-3">{gaps.length?gaps.map((g,i)=><section key={g.skill} className="rounded-2xl border bg-white p-5"><p className="text-sm font-semibold text-cyan-600">WEEK {i+1}</p><h2 className="mt-1 text-xl font-bold">Focus: {g.skill}</h2><p className="mt-1 text-sm text-slate-500">Current evidence: {g.score}% · {g.source}</p><ul className="mt-3 list-disc space-y-1 pl-5 text-slate-600"><li>Review core fundamentals and make concise notes.</li><li>Complete focused practice questions for {g.skill}.</li><li>Retake the relevant assessment and compare the new evidence.</li></ul></section>):<section className="rounded-2xl border bg-white p-6 text-slate-500">No priority gap detected. Continue practice and strengthen project evidence.</section>}</div>}</div>}
